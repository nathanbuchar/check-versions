{
  "name": "check-versions",
  "version": "1.0.0",
  "description": "Checks local Node and npm versions and tells you if they're out of date.",
  "main": "lib/check-versions.js",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "chalk": "^1.1.3",
    "request": "^2.76.0",
    "semver": "^5.3.0"
  },
  "bin": {
    "check-versions": "lib/check-versions.js"
  }
}
