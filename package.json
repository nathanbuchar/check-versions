{
  "name": "check-versions",
  "version": "1.0.0",
  "description": "Checks local Node, npm, and Git versions and tells you if any are out of date.",
  "main": "lib/check-versions.js",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "chalk": "^1.1.3",
    "cli-spinner": "^0.2.5",
    "for-each-async": "^0.1.1",
    "request": "^2.76.0",
    "semver": "^5.3.0"
  },
  "bin": {
    "check-versions": "lib/check-versions.js"
  }
}
